name: Scrape Food Blogs

on:
  schedule:
    - cron: '0 23 * * *'   # 每天 07:00 台灣時間（UTC 23:00 前一天）
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run scraper
        working-directory: line-bot
        run: python scrape_food_blogs.py

      - name: Commit and push
        working-directory: line-bot
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add food_blog_cache.json
          if git diff --cached --quiet; then
            echo "food_blog_cache.json 無變化，略過 commit"
          else
            git commit -m "chore: 自動更新 food_blog_cache.json [skip ci]"
            git push origin HEAD:main
          fi
